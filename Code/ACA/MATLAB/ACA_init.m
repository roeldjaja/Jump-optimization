% ACA_init.m
% Load the required paths and initialise parameters for the ACA package

% Include directory for common functions
addpath(genpath('tools'));

% Include Simulink libraries folder
addpath(genpath('lib'));

% Include parameters dir
addpath(genpath('params'));